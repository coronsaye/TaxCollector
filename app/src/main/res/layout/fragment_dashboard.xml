<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_dashboard"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.vatebra.eirsagentpoc.dashboard.DashboardActivity">

    <TextView
        android:id="@+id/account_balance"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_alignParentTop="true"
        android:background="@drawable/transparent_button"
        android:drawingCacheQuality="high"
        android:elevation="8dp"
        android:gravity="center"
        tools:text="Account Balance: N2000"
        android:textColor="@color/colorPrimaryDark" />

    <com.diegodobelo.expandingview.ExpandingList
        android:id="@+id/expanding_list_main"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/account_balance" />

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_below="@id/expanding_list_main"
        android:layout_centerHorizontal="true"
        android:src="@drawable/edologo" />


</RelativeLayout>
